#!/bin/sh
if [ "_$1_" != "__" ]; then
	if [ "_$2_" != "__" ]; then
		if [ "_$3_" != "__" ]; then
			if [ "_$4_" != "__" ]; then
				if [ "_$5_" != "__" ]; then
					echo "[[ $1 $2 $3 $4 $5 ]]"	
				else
					echo "[[ $1 $2 $3 $4 ]]"
				fi
			else
				echo "[[ $1 $2 $3 ]]"
			fi
		else
			echo "[[ $1 $2 ]]"
		fi
	else
		echo "[[ $1 ]]"
	fi
else
	echo "[[ ]]"
fi
