#!/bin/sh
DB=hakemisto.db
if [ -z "$1" ]; then
	SUMMA="0,0"
else
	SUMMA=$(summa "$1")
fi
#DB=$DB TABLE=Documents kanta -s:summa "$SUMMA"
echo DUPLICATE $UMMA

if [ "$SUMMA" = "0,0" ]; then
	nada; DB=$DB TABLE=Documents kanta -s:M2|grep M2
else
	nada
fi