#!/usr/bin/env ruby
# frozen_string_literal: true

require 'thor'
#require 'fileutils'

# single line text editor for the command line. work in progress
class Singular < Thor
  default_task :edit

	desc 'edit', 'edit text file.'
	def edit(filename)
		puts "Whats a name?"
		name = $stdin.readline
		puts "name is " + name
	end

end

Singular.start(ARGV)
