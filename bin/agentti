#!/bin/bash

case $1 in
	pull) echo AGENTTI: git pull;;
	push) echo AGENTTI: git push;;
	*) if [ "_$SSH_AGENT_PID" == "_" ]; then ps aux|grep ssh|grep agent|grep -v grep|awk '{print $1}'; else echo $SSH_AGENT_PID; fi;;
esac
