#!/bin/sh
COUNT="0"
if [ -z "$DB" ]; then DB="$(asetus path:db)"; fi
if [ -z "$TABLE" ]; then TABLE="$(asetus str:table)"; fi
#COUNT=$(DB=$DB kanta -t:$TABLE -f $1|grep $1|wc -l|tr -s '')
case "$COUNT" in
	"0") printf "false\n";;
	*) printf "true\n";;
esac
