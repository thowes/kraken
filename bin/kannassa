#!/bin/sh
COUNT="0"
if [ -z "$DB" ]; then DB="$(asetus path:db)"; fi
if [ -z "$TABLE" ]; then TABLE="$(asetus str:table)"; fi
echo DB=$DB kanta -t:$TABLE -f $1
case "$COUNT" in
	"0") printf "false\n";;
	*) printf "true\n";;
esac
