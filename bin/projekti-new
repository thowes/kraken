#!/usr/bin/env ruby
# frozen_string_literal: true

require 'thor'
# require 'fileutils'

# PROJEKTI class: list projects and their directories.
class Projekti < Thor
  default_task :tila

  desc 'go', 'go to the project directory.'
  def go
    #
  end

  private

  def tila
    status = "$(git status | \grep 'On branch')"
    puts status if status.include?('master')
  end

end

Projekti.start(ARGV)
